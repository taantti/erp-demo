import { createTenant, readTenant, readTenants, updateTenant, deleteTenant } from './tenantService.js';

export default {
    createTenant,
    readTenant,
    readTenants,
    updateTenant,
    deleteTenant
}
