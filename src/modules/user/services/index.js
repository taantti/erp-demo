import { createUser, readUser, readUsers, updateUser, updateUserPassword, deleteUser } from './userService.js';

export default {
    createUser,
    readUser,
    readUsers,
    updateUser,
    updateUserPassword,
    deleteUser
}
