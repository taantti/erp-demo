import { createCustomer, readCustomer, readCustomers, updateCustomer, deleteCustomer } from './customerService.js';

export const customerService = {
    createCustomer,
    readCustomer,
    readCustomers,
    updateCustomer,
    deleteCustomer
};


